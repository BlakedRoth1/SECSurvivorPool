specVersion: "v1"
serviceId: "sec-survivor-pool"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "sec-survivor-pool-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
